'use strict';

angular.module( 'dateaWebApp' )
.controller( 'ChangePasswordCtrl'
, [ '$scope'
	, 'Api'
	, 'User'
	, '$location'
	, 'config'
	, '$routeParams'
, function (
		$scope
	, Api
	, User
	, $location
	, config
	, $routeParams
) {

	$scope.flow     = {};
	$scope.alerts   = [];

	console.log()

} ] );
